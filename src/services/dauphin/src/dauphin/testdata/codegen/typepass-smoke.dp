import "lib:std";
use "std";

enum etest { X: string, Y: boolean };
struct stest { A: vec(number), B: etest };

x := 1;
y := true;
//z := a;
z := [1,2,3];
m := [["hello","world"]];
x (+=) 3;
//z (+=) 2;
n := stest { B: etest:X "hi", A: z };
x := x + 2;
z := *z[@>1];
