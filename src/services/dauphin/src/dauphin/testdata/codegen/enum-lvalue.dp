enum etest {
    X: boolean,
    Y: boolean
};

proc assign(out _A, _A);
inline ":=" assign left 14;
proc assert(boolean,boolean);

x := etest:X false;
x!X := true;
assert(x!X,true);
x!Y := false;
assert(x!X,true);
assert(x!Y,true);
assert(x!Y,false);
