import "lib:std";
use "std";

enum etest {
    X: boolean,
    Y: boolean
};

x := [ etest:X false, etest:X true, etest:X false, etest:Y true ];

x[@<2]!X := true;

assert(x[@==2]!X,false);
assert(x[@==0]!X,true);
