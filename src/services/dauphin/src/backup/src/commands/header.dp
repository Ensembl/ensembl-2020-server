module "buildtime";

func load_ini(string,string,string) becomes string;
func dump_sig(_) becomes string;

func get_size_hint(_) becomes number;
proc set_size_hint(mask _,number);

func is_defined(string) becomes boolean;
func get_define(string) becomes string;

struct version {
    major: number,
    minor: number
};

func get_version(string) becomes version;

proc force_pause();