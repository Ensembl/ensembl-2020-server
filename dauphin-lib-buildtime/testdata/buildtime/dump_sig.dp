import "lib:std";
use "std";

import "lib:buildtime";
use "buildtime";

struct A { x: vec(number) };

m:= A { x: [1,2,3] };
print(dump_sig(m));

struct B { y: number };

n := [B {y: 1}, B {y: 2}, B{ y: 3 }];
print(dump_sig(n));

p := [m,m];
print(dump_sig(p));